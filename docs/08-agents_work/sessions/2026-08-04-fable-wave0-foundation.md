---
date: 2026-08-04
role: frontend-engineer (fable-wave0)
task: wave-0-foundation
branch: feat/wave-0-foundation
qa_verdict: CEO-ACCEPTED 2026-08-04 — formal QA-Lead tiering pending (see HAND-FORWARD)
---

Wave 0 shipped: 27 keyed material assets (webp, replacing 60 MB unkeyed PNGs), DECO tokens moved to `:root` as place-not-mode constants, three hand fonts declared in apps/web (not the kit root), five primitives (Mounted/Taped/Pinned/Torn/Seam) corrected against the revised §1, bench at `/dev/materials`, screenshots day+night in `screens/2026-08-04-wave0/`.
Collision note: a second agent (design-lead-wave0) built in the same worktree from the stale packet; their good work was kept, their asset/token/font/Seam errors corrected — commits interleave.
Seam final: generated `seam-tear-coldpress.webp` once at full width (`variant` exposes `bone`); falloff shaped against four screenshot geometries — steep under the fibre (day leaks page-light otherwise), long into the deep end (night needs the distance), light kept on the torn lip; rejected directions committed as `-falloff-at-tear` / `-falloff-long` frames.
Substrate + lamp per CEO ruling: `<Paper>` renders paper-bone-laid (runtime webp, 134% grain-matched), strip tone-graded to it; `--lamp-dim` drives `.under-lamp` on every material so the table dims as one surface (0.78 — reading room, not lights-off); photographs never carry it. Join measured: day step 27.6→0.6 (sd delta 1.52), night 188→0.1 (0.34).
Night de-inverted per D1: PAPER tokens dim (~×0.73, AA-bounded — ink 8.5:1) instead of flipping dark; inversion-era overrides deleted (pill, tile-ink, shadows, grain, selection); lamp follows to ×0.73. Measured: note 182 vs table 173 (un-inverted), join steps 0.6/0.5.
CEO's coldpress stock adopted on A/B evidence (day delta 1.52→1.02, step 0.6→0.2; night 1.17→0.75, 0.5→0.1) at 58% grain-matched scale, mirror-stacked runtime tile (raw vertical wrap unseamless); bone-laid remains selectable.
**HAND-FORWARD (QA-Lead must tier this; Wave 1 must not discover it as a surprise):** the D1 night de-inversion changes the night appearance of EVERY existing surface — dock, login, echo, today — none of which were reviewed under the new tokens. ~140 lines of inversion-management CSS were deleted (pill fill, tile-ink double, night shadows/grain/selection, lightened authorship inks); any surface that relied on them looks different at night now. Deliberate, per D1 — but unreviewed outside the bench.
Remaining debts: rose/star sliver fix is runtime-only (docs sources still carry the artifacts — upstream fix owed by design-lead); 11pm test re-runs in Wave 1 with a real photograph.
